# 26w17a

## 优化

- 电压连线回显从扁平日志行改为边框盒包裹（7 行：顶边框 + 3 配对行 + 分隔线 + 累计和 + 底边框）
- 乘数确认后黄色闪烁 3 次，闪烁期间等待动画延迟
- 累计数后台线程逐帧递增动画（30ms/帧），与乘数闪烁并行执行
- 累计数未达标黄色、达标绿色；游戏结束绿色文本
- 边框盒预计算固定宽度，内容与边框间保留 1 字符安全边距
- 移除可用符号行和目标值日志行（目标值已在框内累计和行显示）
- 注入 `C_BORDER`、`C_BLUE` 颜色常量到 task.py
