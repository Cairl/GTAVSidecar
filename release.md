# 26w19g

### 添加

- **新增 BRUTEFORCE 黑客游戏自动破解** — 自动检测当前激活方框，红色字母滑入时按回车，8 个方框全部完成后自动结束

### 修复

- 修复完成第 1 个方框后无法继续第 2 个的问题 — 方框切换过渡帧被误判为失败导致重置
- 修复程序反应过慢导致红色字母滑过多个才可能选中的问题 — 改用坐标采样替代覆盖图匹配，扫描间隔从 100ms 降至 50ms
