# 26w19b

### 优化

- **性能面板 CPU 读数更稳定** — 修复了辅助工具空闲时 CPU 使用率突然飙高又归零的问题，现在静态状态下的资源占用更加平稳
- **日志写入改为批量缓冲** — 日志不再每次实时写入磁盘，而是先缓冲到内存再定期批量保存，减少磁盘 I/O 开销
- **界面渲染更智能** — 没有按键操作或界面变化时，菜单刷新频率自动降低，避免不必要的计算

- **任务结束退出游戏的日志回显更清晰** — 检测到任务结束画面时的提示文字从"任务结束"改为"检测到任务结束"，语义更明确
- **成功退出游戏后日志显示为绿色** — 游戏进程成功终止后的回显文字现在以绿色高亮显示，在日志流中更容易辨认成功状态

