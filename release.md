# 26w19f

### 优化

- **集中管理所有终端颜色定义** — 所有颜色码统一由核心模块定义，其他位置不再硬编码颜色值，修改颜色只需改一处
- **消除多处重复逻辑** — 任务组判断、子任务名提取等重复代码统一为共用方法，减少维护成本
- **配置系统性能优化** — 任务注册信息改为缓存复用，不再每次读取配置时重复扫描任务目录
- **日志模块封装改进** — 日志操作改为通过公开接口访问，不再直接操作内部对象
- **清理遗留代码** — 移除已完成的一次性配置迁移代码、无消费者的废弃逻辑、任务硬编码特殊处理等
- **游戏状态检测去重** — 性能面板的游戏状态检测改为复用生命周期管理器的状态，不再独立重复查询进程和窗口
