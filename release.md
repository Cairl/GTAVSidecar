# 26w18e

### 添加

- **新增"显示当前占用性能"开关**，在菜单中按回车即可切换显示或隐藏 CPU、内存占用信息，关闭时还能节省程序性能开销
- **运行中的任务执行前都会自动切回游戏并点击窗口中心**，解决了部分情况下按键没有生效的问题

### 修复

- 创建仅限邀请战局时日志信息从通用提示改为"开始创建"和"创建完成"

### 优化

- 整理程序结构，将核心代码拆分到专用模块中，为后续新增功能打好基础
- 性能信息行靠左对齐，不再撑宽菜单面板
- 菜单面板宽度由任务名称长度决定，不再受底部性能信息影响
- 新建任务后无需手动编辑配置文件，程序会自动检测并添加
