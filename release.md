# 26w19e

### 修复

- **修复性能面板 CPU 占用显示不准确的问题** — 程序内显示的 CPU 占用率（如 34%）与任务管理器（如 2.6%）不一致。现已统一计算方式，显示值与系统任务管理器保持一致

### 添加

- **游戏退出后自动暂停所有检测任务** — 关闭 GTA5 后，所有画面检测任务自动停止轮询，仅保留低频进程检测（每 5 秒一次）。重新启动游戏后，已启用的任务自动恢复检测，无需手动重新开启
- **新增任务自动发现注册机制** — 新增任务时不再需要手动修改核心代码。只需在 `tasks/` 目录下创建任务文件夹，程序启动时自动扫描、注册、排序并加载配置

### 优化

- **统一游戏生命周期管理** — 引入 GameLifecycleManager 集中监控游戏进程状态，替代各任务独立检测窗口的逻辑，减少游戏未运行时的 CPU 开销
- **简化任务配置系统** — 移除硬编码的任务排序列表，改用自动排序。新增 `manifest.json` 支持（可选），任务可自带翻译和默认配置，进一步降低扩展门槛
