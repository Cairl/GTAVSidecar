# 26w18f

### 添加

- **连接主机破解速度现在可以通过 `scan_ms` 统一调节**，调快后按键间隔、动画等待和屏幕重读都会同步加速
- 连接主机 8×10 网格中，目标数字以红色字体直接标识，当前光标以深灰色背景连续色块高亮，重合时灰底红字

### 修复

- **连接主机任务偶发性"崩溃"和回显消失** — 截图失败时不再直接停止任务，而是自动重试
- 连接主机网格刷新不完整的问题，现在始终一次性统筹刷新全部 8 行
- 移除多余的 trigger 检测回显，避免 "连接主机" 和 "游戏开始" 重复出现

### 优化

- 连接主机显示清除逻辑改进，不再残留只有时间戳的空行
