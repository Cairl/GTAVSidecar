# 26w18l

### 添加

- 连接主机破解过程实时显示完整 8×10 数字网格，光标覆盖的 4 个格子以反色高亮
- 程序运行时自动保存操作日志到 `logs` 文件夹，保留最近 5 次会话，颜色标记已去除

### 修复

- **连接主机和电压连线任务现在可以先开启、再等待游戏内图标出现，无需先看到图标才能开启**
- 程序无法通过 Ctrl+C 或 Esc 键意外退出

### 优化

- 连接主机破解回显改为流式显示：游戏开始 → 动态网格（光标高亮实时刷新）→ 游戏结束，不再有持续刷新的状态文本
