# 26w19a

### 修复

- **连接主机任务结束后无法再次正常启动** — 修复任务停止后内部状态未重置的问题，现在重新启用后会正确等待游戏内黑客图标出现，不再错误输出"目标识别失败"
- **连接主机任务结束后偶发崩溃报错** — 修复 `run_once` 任务在工作线程内调用 `stop()` 导致的 `cannot join current thread` 错误

### 优化

- 电压连线每次配对后的等待间隔从 3 秒缩短为 2.5 秒
