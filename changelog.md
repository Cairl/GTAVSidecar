### 添加

- **BRUTEFORCE**: 新增暴力破解黑客任务，自动检测活跃方框内的红色字符并按回车完成破解，支持 8 个位置顺序破解与失败自动重置

### 修复

- **BRUTEFORCE**: 方框过渡期间不再误判为失败重置，确保 8 个位置全部顺序完成
- **BRUTEFORCE**: 修复循环计数器导致过早退出，避免只完成第一个方框后任务停止

### 优化

- **BRUTEFORCE**: 活跃方框检测从覆盖图匹配改为坐标采样，响应速度大幅提升
- **BRUTEFORCE**: 扫描间隔从 100ms 降至 50ms，更快捕捉红色字符